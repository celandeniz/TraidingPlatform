alpaca-py>=0.33
pandas>=2.2
numpy>=1.26
pydantic-settings>=2.4
pyyaml>=6.0
pytest>=8.0
fastapi>=0.115
uvicorn[standard]>=0.30
anthropic>=0.40
ccxt>=4.5
requests>=2.31
statsmodels>=0.14   # pairs stat-arb: Engle-Granger cointegration test

# --- Optional (Phase A: OpenAlice merge). Install when the feature is enabled; ---
# --- core runner/backtest/tests do not import these unless the flag is on.     ---
# openbb>=4.3          # marketdata.enabled: cross-asset data + fundamentals
# ib_async>=1.0        # brokers.*.executor: ib  (Interactive Brokers)
# croniter>=2.0        # scheduler: cron-expression schedules
# feedparser>=6.0      # news_rss.enabled: RSS news aggregation
# mcp>=1.2             # backend.mcp.server: expose tools to external agents
# perspective-python>=3.1  # perspective.enabled: streaming dashboard tables (FINOS)
# gs-quant>=1.0        # research.gs_quant: offline options/Greeks analytics (Goldman)
# pyflyby>=1.9         # dev-only: auto-import in IPython/Jupyter research notebooks (D.E. Shaw)
# yfinance>=0.2.40     # marketdata.backend=yahoo + Yahoo Finance news (free, no key)
# google-genai>=0.3    # research.llm.provider=gemini (Google Gemini)
# python-docx>=1.1     # report export to Word (.docx)
# reportlab>=4.0       # report export to PDF
# (DeepSeek/Qwen provider uses requests — no extra dependency)
# lxml>=5.0             # only for scripts/refresh_universe.py (NASDAQ-100 from Wikipedia)
