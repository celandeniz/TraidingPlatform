import { PlaceholderPage } from "@/components/core/placeholder-page";

export default function BacktestPage() {
  return <PlaceholderPage title="Backtest & Validation" description="Scenario runs, saved grids, and walk-forward validation." endpoint="/api/backtest, /api/walkforward" />;
}
