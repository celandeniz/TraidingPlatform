import { PlaceholderPage } from "@/components/core/placeholder-page";

export default function AutomationPage() {
  return <PlaceholderPage title="Automation" description="Scheduler, webhooks, auto-trader state, and inbox workflows." endpoint="/api/config, /api/inbox, /api/hooks/{name}" />;
}
